// import theme from "styled-theming"
import { colors } from './colors';

export const themes = {
  shifter: {
    colors: {
      primary: colors.magenta,
    },
  },
  amimoto: {
    colors: {
      primary: colors.blue,
    },
  },
};
