import React from 'react';

import { Hero } from '../../views/Hero';
import { Layout } from '../../views/Layout';

export const Post = () => {
  return (
    <Layout>
      <Hero />
    </Layout>
  );
};
