import React, { FC } from 'react';
import { Main } from '@galaxy/views';

export const PageSiteEmergencyAction: FC = () => {
  return (
    <Main title="Emergency Action">
        <p>Silence is golden.</p>
    </Main>
  );
};

export type PageSiteEmergencyActionProps = {
};