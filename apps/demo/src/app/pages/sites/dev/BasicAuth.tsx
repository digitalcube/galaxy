import React, { FC } from 'react';
import { Main } from '@galaxy/views';

export const PageSiteBasicAuth: FC = () => {
  return (
    <Main title="Basic Auth">
        <p>Silence is golden.</p>
    </Main>
  );
};

export type PageSiteBasicAuthProps = {
};