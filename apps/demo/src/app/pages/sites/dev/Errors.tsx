import React, { FC } from 'react';
import { Main } from '@galaxy/views';

export const PageSiteErrors: FC = () => {
  return (
    <Main title="Errors">
        <p>Silence is golden.</p>
    </Main>
  );
};

export type PageSiteErrorsProps = {
};