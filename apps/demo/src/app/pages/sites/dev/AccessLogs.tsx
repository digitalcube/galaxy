import React, { FC } from 'react';
import { Main } from '@galaxy/views';

export const PageSiteAccessLogs: FC = () => {
  return (
    <Main title="Access Logs">
        <p>Silence is golden.</p>
    </Main>
  );
};

export type PageSiteAccessLogsProps = {
};