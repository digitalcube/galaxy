import React, { FC } from 'react';
import { Main } from '@galaxy/views';

export const PageSiteAnalytics: FC = () => {
  return (
    <Main title="Analytics">
        <p>Silence is golden.</p>
    </Main>
  );
};

export type PageSiteAnalyticsProps = {
};