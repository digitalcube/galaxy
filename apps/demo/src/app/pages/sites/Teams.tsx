import React, { FC } from 'react';
import { Main } from '@galaxy/views';

export const PageSiteTeams: FC = () => {
  return (
    <Main title="Teams">
        <p>Silence is golden.</p>
    </Main>
  );
};

export type PageSiteTeamsProps = {
};