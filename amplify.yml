version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run export
  artifacts:
    baseDirectory: exported
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

