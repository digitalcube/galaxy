version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx nx run frontend:build
  artifacts:
    baseDirectory: dist/apps/frontend/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

