version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx nx run frontend:export
  artifacts:
    baseDirectory: dist/apps/frontend/exported
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

