version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx nx run frontend:export
  artifacts:
    baseDirectory: exported
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

