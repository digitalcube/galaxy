import React from 'react';
import { ButtonMenu, ButtonMenuProps } from './button-menu';

export default {
  component: ButtonMenu,
  title: 'Galaxy/Components/ButtonMenu',
};

export const primary = () => {
  /* eslint-disable-next-line */
  const props: ButtonMenuProps = {};

  return <ButtonMenu />;
};
