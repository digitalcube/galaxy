import React from 'react';
import { Siva, SivaProps } from './siva';

export default {
  component: Siva,
  title: 'Siva',
};

export const primary = () => {
  /* eslint-disable-next-line */
  const props: SivaProps = {};

  return <Siva />;
};
